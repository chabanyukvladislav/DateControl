<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DateControl"
             x:Class="DateControl.AddEventPage"
             x:Name="page">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding Description}"/>
            <local:ColorPicker Color="{Binding Color}" HorizontalOptions="Center"/>
            <TimePicker Time="{Binding Time}" />
            <StackLayout x:Name="stack" Orientation="Horizontal" HorizontalOptions="Center" Margin="5"/>
            <Button Command="{Binding Ok}" Text="Ok" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>